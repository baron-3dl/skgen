# compress.compile.md

Compile report for `compress.skill.src`. Target: claude-opus-4-8, Claude Code
harness, this local context. Generated by the compile loop in
`seed/compiler.skill.md`.

## What carried, what bound

- Carried: the prose-compression method, the retention rules, the one-crisp-clause
  mechanism rule, the ratio target, the checks, and the FeedCard pin, all inlined
  from the source. Pure carry.
- Bound: none. A transcript carries its own context, so there was nothing to
  resolve against the local target. The compile is the tuning loop.

## The loop

Steering started from a weak seed ("Summarize the session transcript") so the
convergence is real. Each round: run the current steering on the pinned transcript
with a fresh agent, blind to the reference; score the output against the reference
and the checks; rewrite the steering to close the largest gaps; run again.

| round | score | what the round fixed |
|-------|-------|----------------------|
| 0 | 0.55 | weak seed: labeled output ("Session summary:", "The assistant confirmed"), generic paraphrase, dropped the threshold values and the swiped='left' assignment. |
| 1 | 0.82 | added plain-prose-no-labels and verbatim-token retention. Still wrote "-threshold" instead of dx < -80, and contradicted itself: said right swipe "does nothing" then described the code as setting swiped='right'. |
| 2 | 0.85 | added the one-crisp-clause mechanism rule and describe-by-new-behavior, so dx > 80 reads as "now a no-op" and each condition is stated once. |
| 3 | 0.85 | plateau: tightened the baseline to a single brief parenthetical and cut explanatory asides. |
| 4 | 0.90 | added use-the-concrete-value-even-when-the-source-says-"threshold" and tightened the ratio target. Converged. |

Score climbed 0.55 to 0.90 over five rounds. Converged.

## Final pinned output

Run blind on the FeedCard transcript, the tuned steering produced:

> User clarified swipe intent for resolved cards in /home/baron/projects/mallcop-cloud/dashboard/src/components/FeedCard.jsx: left should reopen ("I don't agree with the resolution"), right should do nothing (the existing labels had resolved cards swiping both directions). Changed onPointerUp so dx<-80 && !isAcked sets swiped='left' and reopens, dx>80 is now a no-op, and updated the swipe zone labels to match; open cards unchanged. All 64 tests passed (12.6s). Committed [main bd7d442] fix(dashboard): resolved cards swipe left to reopen, right does nothing, 1 file changed, 27 insertions, 14 deletions, pushed 0d13a60..bd7d442 to github.com:3dl-dev/mallcop-cloud.git, deployed. After deploy, user opened the dashboard and got "no repos connected" even though it was working a few hours ago, an unresolved follow-on.

score: 0.90

Every load-bearing token survives: the path, the dx<-80 / dx>80 conditions,
!isAcked, swiped='left', the commit hash bd7d442, the push range
0d13a60..bd7d442, the remote, the test count and time, the insertion/deletion
counts, and the "no repos connected" follow-on. The change is stated once by its
new behavior, and the prose is in ratio.

## Checks

Mechanical checks pass: verbatim retention of paths, hashes, values, and code
conditions; plain prose with no labels; no em-dash. Behavioral checks pass: the
change is stated once by its new behavior with no contradiction, the unresolved
follow-on is carried, and the output lands in the target ratio.
