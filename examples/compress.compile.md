# compress.compile.md

Compile report for `compress.skill.src`. Target: claude-opus-4-8, Claude Code
harness, this local context. Generated by the compile loop in
`seed/compiler.skill.md`.

## What carried, what bound

- Carried: the prose-compression method, the retention rules, the one-crisp-clause
  mechanism rule, the source-check decision rule, the ratio target, the checks, and
  the two pins, all inlined from the source.
- Bound: `source-check-tools`. On this target it resolved to:
  - diff-by-revision: `git show <hash>` / `git diff <rev>`
  - tracker show-by-ID: `rd show <id>`
  - standing-instructions file: `/home/baron/.claude/CLAUDE.md` (plus the project CLAUDE.md)

  The decision rule (drop what a local tool reconstructs, keep the verbatim pointer
  plus the reasoning) is carried; only which tools resolve it is local. A recipient
  with a different tracker, VCS, or standing-instructions file reconstructs
  different things, so the compiled steering names the tools that resolved here.

## The loop

Steering seeded from the prior converged prose method, which handled the
self-contained pin but knew nothing of source-check. The loop had to add the bind
behavior without breaking the self-contained case. Each round: run the steering on
both pinned inputs with a fresh agent blind to the references; score each against
its reference and the checks; rewrite to close the largest gaps; run again.

| round | mean | feedcard | sourcecheck | what the round did |
|-------|------|----------|-------------|--------------------|
| 0 | 0.48 | 0.55 | 0.40 | seed knows nothing of source-check, so it kept the nalu-3f2 spec, the full commit body, and the CLAUDE.md constraint verbatim. The bind-exercising pin scored 0.40 for over-retention. |
| 1 | 0.77 | 0.63 | 0.90 | added source-check with the resolved tool names. The bind pin jumped to 0.90, but feedcard FELL to 0.63: it began dropping reconstructable-looking content on a self-contained transcript where nothing is actually reconstructable. |
| 2 | 0.89 | 0.85 | 0.92 | reconciled: source-check fires only on what a named bound tool actually regenerates from a pointer the transcript already carries; a self-contained transcript drops nothing. Both rose. Converged. |

The middle-round dip is the bind earning its place. Adding source-check is not free:
the first attempt over-fired, and the gap the loop had to close was teaching the
steering when NOT to drop. The seed knew only "keep everything"; the converged
steering knows "drop what a named local tool can regenerate, keep what nothing can."

## Final pinned outputs

Pin 1 (feedcard, self-contained, source-check does NOT fire), run blind:

> Working in /home/baron/projects/mallcop-cloud/dashboard/src/components/FeedCard.jsx, the user clarified that resolved (acked) cards should reopen on a left swipe (kick the item back to "open"/"unresolved", meaning "I don't agree with the resolution") and do nothing on a right swipe ... The suite reported 64 passed (12.6s). Committed [main bd7d442] fix(dashboard): resolved cards swipe left to reopen, right does nothing, 1 file changed, 27 insertions, 14 deletions, pushed 0d13a60..bd7d442 to github.com:3dl-dev/mallcop-cloud.git ... The user then opened the dashboard and saw "no repos connected" even though this was working a few hours ago, which remains open.

score: 0.85. Every load-bearing token survives and nothing is dropped, which is
correct: this transcript is its own only record. Residual: the pin's input code
block uses the symbolic `dx<-threshold`/`dx>threshold` and shows the right-swipe
path as `swiped='right'/expanded`, which conflicts with the user's stated intent
(right = no-op). The candidate honestly carried the symbol and surfaced the
conflict rather than silently resolving it, which costs points against a gold
written with the real `dx<-80`/`dx>80` values and the intent already resolved. The
divergence is in the pin, not the method.

Pin 2 (sourcecheck, the bind fires), run blind:

> Worked nalu-3f2 (spec reconstructable via rd show), implementing an SNR floor guard on the quantizer, committed as a1b2c3d on branch work/nalu-3f2. The guard's cutoff is 6 dB rather than 8 dB because at 8 dB the sweep killed three configs that converged fine downstream, while 6 dB still cuts the divergent ones and keeps those three.

score: 0.92. The bind did its work: the spec text dropped to the item ID with the
tool named (`reconstructable via rd show`), the change dropped to its hash with the
reasoning kept, and the "never commit straight to main" constraint dropped entirely
because it lives in CLAUDE.md. The verbatim pointers (nalu-3f2, work/nalu-3f2,
a1b2c3d) and the 6-vs-8 reasoning survive, because nothing can regenerate the why.

## Checks

Mechanical checks pass: verbatim retention of non-reconstructable tokens, plain
prose, no em-dash. Behavioral checks pass: source-check fires on the bind pin and
stays out of the way on the self-contained pin, each change is stated once by its
new behavior, and both outputs land in ratio.
